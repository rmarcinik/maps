{
	"scripts": {
		"build": "esbuild src/map.js --bundle --outfile=public/map.js",
		"dev": "npm run build && wrangler dev",
		"release": "esbuild src/map.js --bundle --minify --outfile=public/map.js && wrangler deploy"
	},
	"devDependencies": {
		"@chenglou/pretext": "^0.0.4",
		"esbuild": "*",
		"maplibre-gl": "^5.23.0",
		"wrangler": "^4"
	}
}
